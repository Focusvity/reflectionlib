plugins {
    id 'java'
}

group 'dev.deafkid'
version '1.0-SNAPSHOT'
description 'A simple reflection library'

repositories {
    mavenCentral()
}

java {
    toolchain.languageVersion.set(JavaLanguageVersion.of(17))
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}